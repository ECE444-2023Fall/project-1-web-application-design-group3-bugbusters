version: "3.8"
services:
  flask-server:
    build:
      context: ./flask-server
      args:
        PORT: ${PORT}
    container_name: flask-server-container
    ports:
      - ${PORT}:${PORT}
    volumes:
      - ./flask-server:/app
    stdin_open: true
    tty: true
